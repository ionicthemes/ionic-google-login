export declare class BuildError extends Error {
    hasBeenLogged: boolean;
    constructor(err?: any);
    toJson(): {
        message: string;
        name: string;
        stack: string;
        hasBeenLogged: boolean;
    };
}
export declare class IgnorableError extends Error {
    constructor(msg?: string);
}
export declare class Logger {
    private start;
    private scope;
    constructor(scope: string);
    ready(chalkColor?: Function): void;
    finish(chalkColor?: Function): void;
    private completed(type, chalkColor);
    fail(err: Error): Error;
    /**
     * Does not print out a time prefix or color any text. Only prefix
     * with whitespace so the message is lined up with timestamped logs.
     */
    static log(...msg: any[]): void;
    /**
     * Prints out a dim colored timestamp prefix.
     */
    static info(...msg: any[]): void;
    /**
     * Prints out a yellow colored timestamp prefix.
     */
    static warn(...msg: any[]): void;
    /**
     * Prints out a error colored timestamp prefix.
     */
    static error(...msg: any[]): void;
    /**
     * Prints out a blue colored DEBUG prefix. Only prints out when debug mode.
     */
    static debug(...msg: any[]): void;
    static printDiagnostic(d: Diagnostic): void;
    static highlightError(errorLine: string, errorCharStart: number, errorLength: number): string;
    static removeWhitespaceIndent(lines: PrintLine[]): void;
    static wordWrap(msg: any[]): string[];
    static meaningfulLine(line: string): boolean;
    static jsSyntaxHighlight(text: string): string;
    static cssSyntaxHighlight(text: string, errorCharStart: number): string;
    static formatFileName(rootDir: string, fileName: string): string;
    static formatHeader(task: string, fileName: string, rootDir: string, startLineNumber?: number, endLineNumber?: number): string;
    static newLine(): void;
    static INDENT: string;
    static MAX_LEN: number;
}
export declare function getAppScriptsVersion(): string;
export interface TaskEvent {
    scope: string;
    type: string;
    duration?: number;
    time?: string;
    msg?: string;
}
export interface Diagnostic {
    level: string;
    syntax: string;
    type: string;
    header: string;
    code: string;
    messageText: string;
    fileName: string;
    lines: PrintLine[];
}
export interface PrintLine {
    lineIndex: number;
    lineNumber: number;
    text: string;
    errorCharStart: number;
    errorLength: number;
}
